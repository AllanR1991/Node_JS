<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia perguntas</title>
    <!-- Como estamos trabalhando com express, automaticamente ele ja vai procurar uma pasta css na pasta public, que ira funcionar.  -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Guia perguntas!</h1>
    <hr>
    <h3>Lista de perguntas</h3>


    <p>Nome: <%= nome %></p>
    <p>Linguagem favorita: <%= lang %></p>
    <p>Empresa: <%= empresa %></p>
    <p>Inscritos no canal: <%= incritos %></p>

    <!-- Utilizando IF e ELSE com EJS -->
    <% if (exibeMsg) { %>  
        <h3>Isso é uma mensagem de erro!</h3>
    <% } else { %>
        <h3>Nenhum erro na página</h3>
    <% } %>


    <!-- Utilizando for each com EJS -->
    <ul>
        <% produtos.forEach(produto => { %>
            <li><p><%= produto.nome %></p></li>
            <ul>
                <li><p>Preço:  <%= produto.preco %></p></li>
            </ul>
        <% }) %>
    </ul>

    <img src="/img/carro.jpg" alt="Carro" >
</body>
</html>